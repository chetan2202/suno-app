// Layer 3 — Local persistence. IndexedDB repository API, operation-log storage,
// migrations, local device_id. Exposes a clean repository API to the domain layer
// so the operation log and (v0.2) sync slot in behind it. See architecture.md.
//
// Placeholder for the v0.1 scaffold; the IndexedDB repository lands next.
export {};
