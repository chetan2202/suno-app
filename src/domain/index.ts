// Layer 2 — Domain. Grocery/household rules, operation generation, deterministic
// merge/reducer. Testable without a browser. See architecture.md section 4.
//
// v0.1 build order (plan.md): types are in place; operation generation and the
// reducer land next.
export * from "./types.js";
