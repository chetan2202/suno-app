// Layer 5 — Platform adapter. Capabilities the browser cannot reliably provide
// (local server, Wi-Fi discovery, background service). Isolated behind the
// SyncTransport interface so the core app stays web-based. Deferred to v0.2.
// See architecture.md sections 4 and 9.
export {};
