{
  "name": "suno-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-first family grocery PWA — offline-first, no accounts, no backend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
